// components/Header.js
import React from 'react';

const Header = ({ isDarkMode, toggleDarkMode }) => {
  return (
    <header>
      <h1>Human Space Race</h1>
      
      
    </header>
  );
};

export default Header;



// <button onClick={toggleDarkMode}>
//         {isDarkMode ? 'Light Mode' : 'Dark Mode'} </button>